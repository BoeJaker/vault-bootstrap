# Vault / Docker Secret Bootstrap

Performs the intial configuration of vault and docker, 

Generating user, password and other credentials for dokcer containers at build time and storing them in vault